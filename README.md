# CLB KLB

## Setup

Install dependencies and initialize the project:

```bash
composer setup
```

## Usage

To start the development server, run:

```bash
composer serve
```

This will start the server at `http://localhost:8000`.
You can access the application in your web browser at this address.
